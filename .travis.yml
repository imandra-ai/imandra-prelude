language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-ci-scripts/master/.travis-docker.sh
script: bash -ex .travis-docker.sh
services:
- docker
env:
  global:
  - PINS="imandra-prelude:."
  - DISTRO="ubuntu-16.04"
  matrix:
  - PACKAGE="imandra-prelude" OCAML_VERSION="4.03"
  - PACKAGE="imandra-prelude" OCAML_VERSION="4.04"
  - PACKAGE="imandra-prelude" OCAML_VERSION="4.05"
  - PACKAGE="imandra-prelude" OCAML_VERSION="4.06"
  - PACKAGE="imandra-prelude" OCAML_VERSION="4.07"
